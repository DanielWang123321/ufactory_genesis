# 强化学习示例

v0.2.11 只公开一个边界明确的 RL 示例：
[xArm6 + Gripper G2 固定布局抓取放置](pick_place/README_cn.md)。目录内包含一份规范配方、
固定 512 回合场景库，以及经过完整性校验的 `model_199.pt` 检查点包。

请在仓库根目录通过模块方式运行所有入口。本示例仅支持 Linux + NVIDIA GPU，且不提供
任何 RL 真机执行接口。

[English](README.md)
